---
import Base from './Base.astro'

interface Props {
  title: string
  subtitle: string
  tags: string[]
  year: string
  description?: string
  prevStudy?: { title: string; href: string }
  nextStudy?: { title: string; href: string }
}

const {
  title,
  subtitle,
  tags,
  year,
  description,
  prevStudy,
  nextStudy,
} = Astro.props

const metaDescription = description ?? `${title} — ${subtitle}`
---

<Base title={title} description={metaDescription}>
  <article style="width: 70%; max-width: 1080px; margin: 0 auto; padding: 80px 0 120px;">

    <!-- Breadcrumb -->
    <nav aria-label="Breadcrumb" style="margin-bottom: 48px;">
      <a
        href="/#research"
        style="font-family: 'JetBrains Mono', monospace; font-size: 13px; font-weight: 600; letter-spacing: 0.15em; text-transform: uppercase; color: var(--text-muted); text-decoration: none; transition: color 300ms cubic-bezier(0.2, 0, 0, 1);"
        class="breadcrumb-link"
      >
        <span style="font-size: 1.3em; vertical-align: baseline; margin-right: 4px;">&larr;</span> Research
      </a>
    </nav>

    <!-- Header -->
    <header style="margin-bottom: 48px;">
      <h1 style="font-family: 'Cormorant Garamond', Georgia, serif; font-weight: 400; font-size: clamp(36px, 5vw, 56px); color: var(--text); line-height: 1.1; margin: 0 0 16px;">
        {title}
      </h1>
      <p style="font-family: Inter, sans-serif; font-style: italic; font-size: 18px; color: var(--text-muted); margin: 0 0 20px; line-height: 1.5;">
        {subtitle}
      </p>
      <div style="font-family: 'JetBrains Mono', monospace; font-size: 13px; font-weight: 600; letter-spacing: 0.15em; text-transform: uppercase; color: var(--accent);">
        {tags.join(' \u00B7 ')}
      </div>
    </header>

    <hr style="border: none; border-top: 1px solid var(--rule); margin: 0 0 48px;" />

    <!-- Two-column: prose + sidebar -->
    <div class="cs-grid" style="display: grid; grid-template-columns: 1fr 240px; gap: 80px; align-items: start;">

      <!-- Main prose -->
      <div class="cs-prose" style="max-width: 720px;">
        <slot />
      </div>

      <!-- Sidebar -->
      <aside class="cs-sidebar">
        <slot name="sidebar" />
      </aside>

    </div>

    <!-- Pagination -->
    {(prevStudy || nextStudy) && (
      <>
        <hr style="border: none; border-top: 1px solid var(--rule); margin: 80px 0 40px;" />
        <nav
          style="display: flex; justify-content: space-between; align-items: center; gap: 24px;"
          aria-label="Case study navigation"
        >
          <div>
            {prevStudy && (
              <a
                href={prevStudy.href}
                class="pagination-link"
                style="font-family: 'JetBrains Mono', monospace; font-size: 13px; font-weight: 600; text-transform: uppercase; color: var(--text-muted); text-decoration: none; transition: color 400ms cubic-bezier(0.2, 0, 0, 1);"
              >
                <span style="font-size: 1.3em; vertical-align: baseline; margin-right: 4px;">&larr;</span> {prevStudy.title}
              </a>
            )}
          </div>
          <div>
            {nextStudy && (
              <a
                href={nextStudy.href}
                class="pagination-link"
                style="font-family: 'JetBrains Mono', monospace; font-size: 13px; font-weight: 600; text-transform: uppercase; color: var(--text-muted); text-decoration: none; transition: color 400ms cubic-bezier(0.2, 0, 0, 1); text-align: right;"
              >
                {nextStudy.title} <span style="font-size: 1.3em; vertical-align: baseline; margin-left: 4px;">&rarr;</span>
              </a>
            )}
          </div>
        </nav>
      </>
    )}

  </article>
</Base>

<style>
  /* Prose typography */
  .cs-prose :global(p) {
    font-family: Inter, sans-serif;
    font-size: 17px;
    line-height: 1.75;
    color: var(--text);
    margin-bottom: 32px;
  }

  .cs-prose :global(a) {
    color: inherit;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 4px;
    text-decoration-color: var(--rule);
    transition: all 300ms var(--easing);
  }

  .cs-prose :global(a:hover) {
    color: var(--accent);
    text-decoration-color: var(--accent);
  }

  .cs-prose :global(h2) {
    font-family: 'Cormorant Garamond', Georgia, serif;
    font-weight: 400;
    font-size: 28px;
    color: var(--text);
    margin-top: 48px;
    margin-bottom: 20px;
    line-height: 1.2;
  }

  .cs-prose :global(h3) {
    font-family: Inter, sans-serif;
    font-weight: 600;
    font-size: 15px;
    color: var(--text);
    letter-spacing: 0.05em;
    margin-top: 40px;
    margin-bottom: 16px;
  }

  /* Image + caption */
  .cs-prose :global(.cs-image-wrap) {
    margin: 40px 0 48px;
  }

  .cs-prose :global(.cs-image-grid) {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin: 40px 0 48px;
  }

  .cs-prose :global(.cs-image-grid .cs-image-wrap) {
    margin: 0;
  }

  @media (max-width: 768px) {
    .cs-prose :global(.cs-image-grid) {
      grid-template-columns: 1fr;
      gap: 40px;
    }
  }

  .cs-prose :global(.cs-image-wrap img) {
    width: 100%;
    height: auto;
    border-radius: 2px;
    display: block;
  }

  .cs-prose :global(.cs-caption) {
    font-family: 'JetBrains Mono', monospace;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin: 16px 0 0;
    text-transform: uppercase;
    text-align: center;
  }

  /* Sidebar typography */
  .cs-sidebar :global(.sidebar-block) {
    margin-bottom: 32px;
  }

  .cs-sidebar :global(.sidebar-logo) {
    width: 200px;
    height: auto;
    display: block;
    opacity: 0.6;
    margin-bottom: 12px;
  }

  .cs-sidebar :global(.sidebar-logo.ucsd-health) {
    aspect-ratio: 4 / 1;
    object-fit: cover;
    object-position: 25% center; /* Shift slightly right to crop left space */
    transform: scale(1.1); /* Zoom in a bit more */
  }

  .cs-sidebar :global(.sidebar-label) {
    font-family: 'JetBrains Mono', monospace;
    font-size: 14px;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--text);
    margin-bottom: 8px;
    display: block;
  }

  .cs-sidebar :global(.sidebar-value) {
    font-family: 'JetBrains Mono', monospace;
    font-size: 15px;
    line-height: 1.5;
    color: var(--text-muted);
    display: block;
  }

  .cs-sidebar :global(.sidebar-item) {
    display: block;
    margin-bottom: 12px;
  }

  .cs-sidebar :global(.sidebar-item:last-child) {
    margin-bottom: 0;
  }

  .cs-sidebar :global(.sidebar-link) {
    color: inherit;
    text-decoration: none;
    transition: color 300ms cubic-bezier(0.2, 0, 0, 1);
  }

  .cs-sidebar :global(.sidebar-link:hover) {
    color: var(--accent) !important;
  }

  .cs-sidebar :global(.sidebar-link-icon) {
    font-size: 1.2em;
    margin-left: 4px;
    vertical-align: baseline;
    line-height: 0;
    display: inline-block;
  }

  /* Hover states */
  .breadcrumb-link:hover {
    color: var(--accent) !important;
  }

  .pagination-link:hover {
    color: var(--accent) !important;
  }

  /* Mobile: sidebar below prose */
  @media (max-width: 767px) {
    .cs-grid {
      grid-template-columns: 1fr !important;
      gap: 48px !important;
    }

    .cs-sidebar {
      position: static !important;
    }
  }
</style>
